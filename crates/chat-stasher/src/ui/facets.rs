//! facets — skeleton for the platform/agent facet bar.
//!
//! 29-UI-DESIGN §2 (W137) puts the platform grouping, the facet bar, the
//! machine × source column grouping and the JSON `platform_group` field here.
//! This module is deliberately empty: W170 is a pure move of `ui.rs`, and
//! filling it is a behaviour change that belongs to its own task.
