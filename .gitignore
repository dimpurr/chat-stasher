# 🔴 私有内核 —— 绝不进公开仓
.private/

# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# OS / 编辑器
.DS_Store
*.log

# 扩展（WXT）
apps/extension/node_modules/
apps/extension/.output/
apps/extension/.wxt/

# D15: python bytecode cache from the citation checker
scripts/__pycache__/

# release artifacts produced by scripts/release-artifacts.sh
dist/

# npm packages: nothing in npm/ has dependencies, so a node_modules there is
# always an accident, and a tarball is always a local experiment
npm/node_modules/
npm/**/*.tgz

# A literal `~` directory: a config path that was never tilde-expanded
# must never become a committed file in a public repo.
~/
